class Main {
    function void main() {
        var SquareGame game;
        let game = SquareGame.new();

        //The calling function needs to put the memory address on game
        //onto the global stack first.
        do game.run();

        return;
    }
}
